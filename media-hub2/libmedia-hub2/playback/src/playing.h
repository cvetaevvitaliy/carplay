/*
 * Generated by plugin-codegen.
 */
#ifndef __MH_STATE_PLAYING_H__
#define __MH_STATE_PLAYING_H__

#include <gio/gio.h>
#include "state.h"

/*
 * Type Macros
 */

#define MH_TYPE_STATE_PLAYING \
	(mh_state_playing_get_type())
#define MH_STATE_PLAYING(obj) \
	(G_TYPE_CHECK_INSTANCE_CAST((obj), MH_TYPE_STATE_PLAYING, MHStatePlaying))
#define MH_IS_STATE_PLAYING(obj) \
	(G_TYPE_CHECK_INSTANCE_TYPE((obj), MH_TYPE_STATE_PLAYING))
#define MH_STATE_PLAYING_CLASS(klass) \
	(G_TYPE_CHECK_CLASS_CAST((klass), MH_TYPE_STATE_PLAYING, MHStatePlayingClass))
#define MH_IS_STATE_PLAYING_CLASS(klass) \
	(G_TYPE_CHECK_CLASS_TYPE((klass), MH_TYPE_STATE_PLAYING))
#define MH_STATE_PLAYING_GET_CLASS(obj) \
	(G_TYPE_INSTANCE_GET_CLASS((obj), MH_TYPE_STATE_PLAYING, MHStatePlayingClass))

typedef struct _MHStatePlaying		MHStatePlaying;
typedef struct _MHStatePlayingClass	MHStatePlayingClass;

struct _MHStatePlayingClass
{
	MHStateClass parent_class;

	/* Class Ios */

	/* Class Methods */

	/* Class Properties */
};

struct _MHStatePlaying
{
	MHState parent;

	/* Instance Members */

};

/* used by MH_TYPE_STATE_PLAYING */
GType mh_state_playing_get_type( void );

/* Ios List */
MHStatePlaying * mh_state_playing_instance();

#endif /* __MH_STATE_PLAYING_H__ */
