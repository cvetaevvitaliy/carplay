/*
 * Generated by object-codegen.
 */
#include <glib.h>
#include "stmdefs.h"
#include "ready.h"
#include "switching.h"
#include "error.h"
#include <mh_dev.h>
#include <mh_pb.h>
#include <mh_playlist.h>

typedef struct _MHStateSwitchingPrivate MHStateSwitchingPrivate;

struct _MHStateSwitchingPrivate
{
	gint dummy;
};

G_DEFINE_TYPE_WITH_PRIVATE( MHStateSwitching, mh_state_switching, MH_TYPE_STATE )

static GObject * switchingObject;

/*
 * ===  FUNCTION  ======================================================================
 *         Name:  _action
 *  Description:
 * =====================================================================================
 */
static void _action( MHState * self, MHPb *pb, guint event, gpointer param1, gpointer param2 )
{
	MHPb * _pb = MH_PB( pb );
	switch( event )
	{
		case EVENT_READY:
			g_message( "[switching] to [ready]" );
			mh_pb_dispatch( _pb, EVENT_CHANGE, GUINT_TO_POINTER( READY_STATUS ), NULL );
			break;
		case EVENT_ERROR:
			g_message( "[switching] to [ERROR]" );
			mh_pb_dispatch( _pb, EVENT_CHANGE, GUINT_TO_POINTER( ERROR_STATUS ), NULL );
			break;
		default:
			break;
	}

	//ipod status
	if(( _pb->mhDev	!=	NULL ) && ( g_strcmp0( _pb->mhDev->type , "iap2")==0 ))
	{
		switch( event )
		{
		case EVENT_PLAY:
			g_message( "[switching] to [Playing]" );
			mh_dev_play( _pb->mhDev, _pb );
			break;
		case EVENT_PAUSE:
			g_message( "[switching] to [Pause]" );
			mh_dev_pause( _pb->mhDev, _pb );
			break;
		case EVENT_SWITCHING_NEXT:
			mh_pb_dispatch( _pb, EVENT_CHANGE, GUINT_TO_POINTER( SWITCHING_STATUS ), NULL );
			mh_dev_next( _pb->mhDev, _pb );
			break;
		case EVENT_SWITCHING_PREVIOUS:
			mh_pb_dispatch( _pb, EVENT_CHANGE, GUINT_TO_POINTER( SWITCHING_STATUS ), NULL );
			mh_dev_previous( _pb->mhDev, _pb );
			break;
		case EVENT_SWITCHING_SELECT:
			mh_dev_set_index( _pb->mhDev, _pb, ( uint32_t )_pb->playlist->index );
			break;
		case EVENT_SEEKING:
			g_message( "[switching] to [seeking]" );	
			mh_pb_dispatch( _pb, EVENT_CHANGE, GUINT_TO_POINTER( SEEKING_STATUS ), GUINT_TO_POINTER( PAUSE_STATUS ) );
			mh_dev_seek( _pb->mhDev, _pb, ( uint32_t )_pb->seeking_pos );
			break;
		case EVENT_SEEKING_FORWARD:
			g_message( "[switching] to [seeking]" );	
			mh_dev_forward( _pb->mhDev, _pb );
			break;
		case EVENT_SEEKING_BACKWARD:
			g_message( "[switching] to [seeking]" );	
			mh_dev_backward( _pb->mhDev, _pb );
			break;
		case EVENT_SEEKING_FORWARD_DONE:
			mh_dev_forward_done( _pb->mhDev, _pb );
			break;
		case EVENT_SEEKING_BACKWARD_DONE:
			mh_dev_backward_done( _pb->mhDev, _pb );
			break;

		default:
			break;
		}
	}
	else
	{
		if ( event != EVENT_READY && event != EVENT_ERROR )
			g_message( "not support USB [switching] to [%d]", event);
	}

	//Method default implement.
}      /* -----  end of static function action  ----- */

/*
 * ===  FUNCTION  ======================================================================
 *         Name:  _open
 *  Description:
 * =====================================================================================
 */
static void _open( MHState * self, MHPb * pb )
{
	g_message( "Opening [switching]" );
	mh_pb_state_open( pb, MH_PB_STATE_SWITCHING, NULL);
}      /* -----  end of static function open  ----- */

/*
 * ===  FUNCTION  ======================================================================
 *         Name:  _close
 *  Description:
 * =====================================================================================
 */
static void _close( MHState * self, MHPb * pb )
{
	g_message( "Closing [switching]" );
}      /* -----  end of static function close  ----- */

/*
 * ===  FUNCTION  ======================================================================
 *         Name:  mh_state_switching_dispose
 *  Description:
 * =====================================================================================
 */
static void mh_state_switching_dispose( GObject * object )
{
	MHStateSwitching * _self	=	MH_STATE_SWITCHING( object );

	switchingObject	=	NULL;	

	G_OBJECT_CLASS( mh_state_switching_parent_class )->dispose( object );
}

/*
 * ===  FUNCTION  ======================================================================
 *         Name:  mh_state_switching_finalize
 *  Description:
 * =====================================================================================
 */
static void mh_state_switching_finalize( GObject * object )
{
	MHStateSwitching * _self	=	MH_STATE_SWITCHING( object );
	MHStateSwitchingPrivate * _priv	=	mh_state_switching_get_instance_private( _self );

	G_OBJECT_CLASS( mh_state_switching_parent_class )->finalize( object );
}

/*
 * ===  FUNCTION  ======================================================================
 *         Name:  mh_state_switching_init
 *  Description:
 * =====================================================================================
 */
static void mh_state_switching_init( MHStateSwitching * self )
{
	MHStateSwitchingPrivate * _priv	=	mh_state_switching_get_instance_private( self );
}       /* -----  end of static function mh_state_switching_init  ----- */

/* 
 * ===  FUNCTION  ======================================================================
 *         Name:  mh_state_switching_constructor
 *  Description:  
 * =====================================================================================
 */
static GObject * mh_state_switching_constructor( GType type, guint n_construct_params, 
									GObjectConstructParam * construct_params )
{
	if( switchingObject == NULL )
	{
		switchingObject	=	G_OBJECT_CLASS( mh_state_switching_parent_class )->constructor( type, 
						n_construct_params,	construct_params );
	}

	return switchingObject;
}		/* -----  end of static function mh_state_switching_constructor  ----- */

/*
 * ===  FUNCTION  ======================================================================
 *         Name:  mh_state_switching_class_init
 *  Description:
 * =====================================================================================
 */
static void mh_state_switching_class_init( MHStateSwitchingClass * klass )
{
	MHStateClass * _parentClass	=	MH_STATE_CLASS( klass );
	GObjectClass * _gobjectClass	=	G_OBJECT_CLASS( klass );

	_gobjectClass->constructor	=	mh_state_switching_constructor;
	_gobjectClass->dispose	=	mh_state_switching_dispose;
	_gobjectClass->finalize	=	mh_state_switching_finalize;

	/* assemble ios of mh_state_switching class */

	/* assemble methods of mh_state_switching class */
	_parentClass->action	=	_action;
	_parentClass->open	=	_open;
	_parentClass->close	=	_close;

	/* Ios */
	/* Signals */
}       /* -----  end of static function mh_state_switching_class_init  ----- */

/* 
 * ===  FUNCTION  ======================================================================
 *         Name:  mh_state_switching_instance
 *  Description:  
 * =====================================================================================
 */
MHStateSwitching * mh_state_switching_instance()
{
	return g_object_new( MH_TYPE_STATE_SWITCHING, NULL );
}		/* -----  end of function mh_state_switching_instance  ----- */
