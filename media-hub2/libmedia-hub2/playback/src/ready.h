/*
 * Generated by plugin-codegen.
 */
#ifndef __MH_STATE_READY_H__
#define __MH_STATE_READY_H__

#include <gio/gio.h>
#include "state.h"

/*
 * Type Macros
 */

#define MH_TYPE_STATE_READY \
	(mh_state_ready_get_type())
#define MH_STATE_READY(obj) \
	(G_TYPE_CHECK_INSTANCE_CAST((obj), MH_TYPE_STATE_READY, MHStateReady))
#define MH_IS_STATE_READY(obj) \
	(G_TYPE_CHECK_INSTANCE_TYPE((obj), MH_TYPE_STATE_READY))
#define MH_STATE_READY_CLASS(klass) \
	(G_TYPE_CHECK_CLASS_CAST((klass), MH_TYPE_STATE_READY, MHStateReadyClass))
#define MH_IS_STATE_READY_CLASS(klass) \
	(G_TYPE_CHECK_CLASS_TYPE((klass), MH_TYPE_STATE_READY))
#define MH_STATE_READY_GET_CLASS(obj) \
	(G_TYPE_INSTANCE_GET_CLASS((obj), MH_TYPE_STATE_READY, MHStateReadyClass))

typedef struct _MHStateReady		MHStateReady;
typedef struct _MHStateReadyClass	MHStateReadyClass;

struct _MHStateReadyClass
{
	MHStateClass parent_class;

	/* Class Ios */

	/* Class Methods */

	/* Class Properties */
};

struct _MHStateReady
{
	MHState parent;

	/* Instance Members */

};

/* used by MH_TYPE_STATE_READY */
GType mh_state_ready_get_type( void );

/* Ios List */
MHStateReady * mh_state_ready_instance();
#endif /* __MH_STATE_READY_H__ */
