/*
 * Generated by object-codegen.
 */
#include <glib.h>
#include "stmdefs.h"
#include "pause.h"
#include "switching.h"
#include "seeking.h"
#include "playing.h"
#include <mh_playlist.h>
#include <mh_dev.h>
#include <mh_pb.h>

typedef struct _MHStatePlayingPrivate MHStatePlayingPrivate;

struct _MHStatePlayingPrivate
{
	gint dummy;
};

G_DEFINE_TYPE_WITH_PRIVATE( MHStatePlaying, mh_state_playing, MH_TYPE_STATE )

static GObject * playingObject;

/*
 * ===  FUNCTION  ======================================================================
 *         Name:  _action
 *  Description:
 * =====================================================================================
 */
static void _action( MHState * self, MHPb * pb, guint event, gpointer param1, gpointer param2 )
{
	MHPb * _pb = MH_PB( pb );
	switch( event )
	{
		case EVENT_PAUSE:
			g_message( "[Playing] to [Pause]" );
			mh_dev_pause( _pb->mhDev, _pb );
			break;
		case EVENT_SWITCHING_NEXT:
			g_message( "[Playing] to [switching], EVENT_SWITCHING_NEXT" );			
			mh_pb_dispatch( _pb, EVENT_CHANGE, GUINT_TO_POINTER( SWITCHING_STATUS ), NULL );
			mh_dev_next( _pb->mhDev, _pb );
			break;
		case EVENT_SWITCHING_REPEAT_ONE:
			g_message( "[Playing] to [switching], EVENT_SWITCHING_REPEAT_ONE" );			
			mh_pb_dispatch( _pb, EVENT_CHANGE, GUINT_TO_POINTER( SWITCHING_STATUS ), NULL );
			break;
		case EVENT_SWITCHING_PREVIOUS:
			g_message( "[Playing] to [switching], EVENT_SWITCHING_PREVIOUS" );
			mh_pb_dispatch( _pb, EVENT_CHANGE, GUINT_TO_POINTER( SWITCHING_STATUS ), NULL );
			mh_dev_previous( _pb->mhDev, _pb );
			break;
		case EVENT_SWITCHING_SELECT:
			g_message( "[Playing] to [switching], EVENT_SWITCHING_SELECT" );
			if( _pb->playlist != NULL )
			{
				mh_dev_set_index( _pb->mhDev, _pb, ( uint32_t )_pb->playlist->index );
			}else{
				g_message("playing %s playlist is NULL!!",__func__);		
			}
			break;
		case EVENT_SEEKING:
			g_message( "[Playing] to [seeking]" );	
			mh_pb_dispatch( _pb, EVENT_CHANGE, GUINT_TO_POINTER( SEEKING_STATUS ), GUINT_TO_POINTER( PLAY_STATUS ) );
			mh_dev_seek( _pb->mhDev, _pb, ( uint32_t )_pb->seeking_pos );
			break;
		case EVENT_SEEKING_FORWARD:
			g_message( "[Playing] to [seeking]" );	
			mh_dev_forward( _pb->mhDev, _pb );
			break;
		case EVENT_SEEKING_BACKWARD:
			g_message( "[Playing] to [seeking]" );	
			mh_dev_backward( _pb->mhDev, _pb );
			break;
		case EVENT_SEEKING_FORWARD_DONE:
			mh_dev_forward_done( _pb->mhDev, _pb );
			break;
		case EVENT_SEEKING_BACKWARD_DONE:
			mh_dev_backward_done( _pb->mhDev, _pb );
			break;

		default:
			g_message( "[Playing] to [%d]", event);	
			break;
	}
}      /* -----  end of static function action  ----- */

/*
 * ===  FUNCTION  ======================================================================
 *         Name:  _open
 *  Description:
 * =====================================================================================
 */
static void _open( MHState * self, MHPb * pb )
{
	g_message( "Opening [Playing]" );
	mh_pb_state_open( pb, MH_PB_STATE_PLAYING, NULL );
}      /* -----  end of static function open  ----- */

/*
 * ===  FUNCTION  ======================================================================
 *         Name:  _close
 *  Description:
 * =====================================================================================
 */
static void _close( MHState * self, MHPb * pb )
{
	g_message( "Closing [Playing]" );
}      /* -----  end of static function close  ----- */

/* 
 * ===  FUNCTION  ======================================================================
 *         Name:  mh_state_playing_constructor
 *  Description:  
 * =====================================================================================
 */
static GObject * mh_state_playing_constructor( GType type, guint n_construct_params, 
									GObjectConstructParam * construct_params )
{
	if( playingObject == NULL )
	{
		playingObject	=	G_OBJECT_CLASS( mh_state_playing_parent_class )->constructor( type, 
						n_construct_params,	construct_params );
	}

	return playingObject;
}		/* -----  end of static function mh_state_playing_constructor  ----- */

/*
 * ===  FUNCTION  ======================================================================
 *         Name:  mh_state_playing_dispose
 *  Description:
 * =====================================================================================
 */
static void mh_state_playing_dispose( GObject * object )
{
	MHStatePlaying * _self	=	MH_STATE_PLAYING( object );

	playingObject	=	NULL;

	G_OBJECT_CLASS( mh_state_playing_parent_class )->dispose( object );
}

/*
 * ===  FUNCTION  ======================================================================
 *         Name:  mh_state_playing_finalize
 *  Description:
 * =====================================================================================
 */
static void mh_state_playing_finalize( GObject * object )
{
	MHStatePlaying * _self	=	MH_STATE_PLAYING( object );
	MHStatePlayingPrivate * _priv	=	mh_state_playing_get_instance_private( _self );

	G_OBJECT_CLASS( mh_state_playing_parent_class )->finalize( object );
}

/*
 * ===  FUNCTION  ======================================================================
 *         Name:  mh_state_playing_init
 *  Description:
 * =====================================================================================
 */
static void mh_state_playing_init( MHStatePlaying * self )
{
	MHStatePlayingPrivate * _priv	=	mh_state_playing_get_instance_private( self );
}       /* -----  end of static function mh_state_playing_init  ----- */

/*
 * ===  FUNCTION  ======================================================================
 *         Name:  mh_state_playing_class_init
 *  Description:
 * =====================================================================================
 */
static void mh_state_playing_class_init( MHStatePlayingClass * klass )
{
	MHStateClass * _parentClass	=	MH_STATE_CLASS( klass );
	GObjectClass * _gobjectClass	=	G_OBJECT_CLASS( klass );

	_gobjectClass->constructor	=	mh_state_playing_constructor;
	_gobjectClass->dispose	=	mh_state_playing_dispose;
	_gobjectClass->finalize	=	mh_state_playing_finalize;

	/* assemble ios of mh_state_playing class */

	/* assemble methods of mh_state_playing class */
	_parentClass->action	=	_action;
	_parentClass->open	=	_open;
	_parentClass->close	=	_close;

	/* Ios */
	/* Signals */
}       /* -----  end of static function mh_state_playing_class_init  ----- */

/* 
 * ===  FUNCTION  ======================================================================
 *         Name:  mh_state_playing_instance
 *  Description:  
 * =====================================================================================
 */
MHStatePlaying * mh_state_playing_instance()
{
	return g_object_new( MH_TYPE_STATE_PLAYING, NULL );
}		/* -----  end of function mh_state_playing_instance  ----- */
