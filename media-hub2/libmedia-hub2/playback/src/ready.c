/*
 * Generated by object-codegen.
 */
#include <glib.h>
#include "stmdefs.h"
#include "switching.h"
#include "ready.h"
#include "playing.h"
#include "error.h"
#include <mh_playlist.h>
#include <mh_dev.h>
#include <mh_pb.h>

typedef struct _MHStateReadyPrivate MHStateReadyPrivate;

struct _MHStateReadyPrivate
{
	gint dummy;
};

G_DEFINE_TYPE_WITH_PRIVATE( MHStateReady, mh_state_ready, MH_TYPE_STATE )

static GObject * readyObject;

/*
 * ===  FUNCTION  ======================================================================
 *         Name:  _action
 *  Description:
 * =====================================================================================
 */
static void _action( MHState * self, MHPb * pb, guint event, gpointer param1, gpointer param2 )
{
	MHPb * _pb = MH_PB( pb );
	switch( event )
	{
		case EVENT_PLAY:
			g_message( "[Ready] to [Playing]" );
//			mh_pb_dispatch( _pb, EVENT_CHANGE, mh_state_playing_instance(), NULL );
			mh_dev_play( _pb->mhDev, _pb );
			break;
		case EVENT_SWITCHING_NEXT:
			g_message( "[Ready] to [switching]" );
			mh_pb_dispatch( _pb, EVENT_CHANGE, GUINT_TO_POINTER( SWITCHING_STATUS ), NULL );
			mh_dev_next( _pb->mhDev, _pb );
			break;
		case EVENT_SWITCHING_PREVIOUS:
			g_message( "[Ready] to [switching]" );
			mh_pb_dispatch( _pb, EVENT_CHANGE, GUINT_TO_POINTER( SWITCHING_STATUS ), NULL );
			mh_dev_previous( _pb->mhDev, _pb );
			break;
		case EVENT_SWITCHING_SELECT:
			g_message( "[Ready] to [switching]" );
			if( _pb->playlist != NULL )
			{
				mh_dev_set_index( _pb->mhDev, _pb, ( uint32_t )_pb->playlist->index );
			}else{
				g_message("ready %s playlist is NULL!!",__func__);		
			}
			break;
		case EVENT_ERROR:
			g_message( "[Ready] to [Error]" );
			mh_pb_dispatch( _pb, EVENT_CHANGE, GUINT_TO_POINTER( ERROR_STATUS ), NULL );
			break;
		default:
			break;
	}
}      /* -----  end of static function action  ----- */

/*
 * ===  FUNCTION  ======================================================================
 *         Name:  _open
 *  Description:
 * =====================================================================================
 */
static void _open( MHState * self, MHPb * pb )
{
	g_message( "Opening [Ready]" );
	mh_pb_state_open( pb, MH_PB_STATE_READY, NULL );	
}      /* -----  end of static function open  ----- */

/*
 * ===  FUNCTION  ======================================================================
 *         Name:  _close
 *  Description:
 * =====================================================================================
 */
static void _close( MHState * self, MHPb * pb )
{
	g_message( "Closing [Ready]" );
}      /* -----  end of static function close  ----- */

/* 
 * ===  FUNCTION  ======================================================================
 *         Name:  mh_state_ready_constructor
 *  Description:  
 * =====================================================================================
 */
static GObject * mh_state_ready_constructor( GType type, guint n_construct_params, 
									GObjectConstructParam * construct_params )
{
	if( readyObject == NULL )
	{
		readyObject	=	G_OBJECT_CLASS( mh_state_ready_parent_class )->constructor( type, 
						n_construct_params,	construct_params );
	}

	return readyObject;
}		/* -----  end of static function mh_state_ready_constructor  ----- */

/*
 * ===  FUNCTION  ======================================================================
 *         Name:  mh_state_ready_dispose
 *  Description:
 * =====================================================================================
 */
static void mh_state_ready_dispose( GObject * object )
{
	MHStateReady * _self	=	MH_STATE_READY( object );

	readyObject	=	NULL;

	G_OBJECT_CLASS( mh_state_ready_parent_class )->dispose( object );
}

/*
 * ===  FUNCTION  ======================================================================
 *         Name:  mh_state_ready_finalize
 *  Description:
 * =====================================================================================
 */
static void mh_state_ready_finalize( GObject * object )
{
	MHStateReady * _self	=	MH_STATE_READY( object );
	MHStateReadyPrivate * _priv	=	mh_state_ready_get_instance_private( _self );

	G_OBJECT_CLASS( mh_state_ready_parent_class )->finalize( object );
}

/*
 * ===  FUNCTION  ======================================================================
 *         Name:  mh_state_ready_init
 *  Description:
 * =====================================================================================
 */
static void mh_state_ready_init( MHStateReady * self )
{
	MHStateReadyPrivate * _priv	=	mh_state_ready_get_instance_private( self );
}       /* -----  end of static function mh_state_ready_init  ----- */

/*
 * ===  FUNCTION  ======================================================================
 *         Name:  mh_state_ready_class_init
 *  Description:
 * =====================================================================================
 */
static void mh_state_ready_class_init( MHStateReadyClass * klass )
{
	MHStateClass * _parentClass	=	MH_STATE_CLASS( klass );
	GObjectClass * _gobjectClass	=	G_OBJECT_CLASS( klass );

	_gobjectClass->constructor	=	mh_state_ready_constructor;
	_gobjectClass->dispose	=	mh_state_ready_dispose;
	_gobjectClass->finalize	=	mh_state_ready_finalize;

	/* assemble ios of mh_state_ready class */

	/* assemble methods of mh_state_ready class */
	_parentClass->action	=	_action;
	_parentClass->open	=	_open;
	_parentClass->close	=	_close;

	/* Ios */
	/* Signals */
}       /* -----  end of static function mh_state_ready_class_init  ----- */

/* 
 * ===  FUNCTION  ======================================================================
 *         Name:  mh_state_ready_instance
 *  Description:  
 * =====================================================================================
 */
MHStateReady * mh_state_ready_instance()
{
	return g_object_new( MH_TYPE_STATE_READY, NULL );
}		/* -----  end of function mh_state_ready_instance  ----- */
